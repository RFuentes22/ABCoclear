<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/list_bg"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/medium_size">

        <EditText
            android:layout_width="267dp"
            android:layout_height="wrap_content"
            android:layout_weight="7"
                android:textColor="@color/white"
                android:fontFamily="@font/alphabet_fridge_magnets"
                android:textColorHint="@color/white"
            android:hint="@string/pacient_name" />

        <ImageButton
            android:id="@+id/btn_search_patient"
            android:layout_width="4dp"
            android:layout_height="45dp"
            android:layout_weight="1"
            android:background="@drawable/ic_search_list_patient"

            />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/medium_size">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_patients"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/transparent_0"></androidx.recyclerview.widget.RecyclerView>

    </LinearLayout>

</LinearLayout>